# Specs Index

Tool: tool-neutral (codex, claude, grok, and any CodeNote-routed agent)

## Active Location

Use this directory for process docs. Standard non-requirement work uses one `task-card.md`. Controlled work adds `spec.md`, `plan.md`, `tasks.md`, `verify.md`, and `handoff.md` when needed. Add `changes.md` when delivery spans multiple files.

## Rules

- New tasks follow CodeNote process date grouping: `vibe/specs/<yyMM>/<yyMMdd>/<HHmm-task-id>/`.
- Business changes must link before/after documents and identify authoritative current behavior.
- Run link validation for documentation-heavy changes.
- Current hub: [PROJECT_STATUS.md](PROJECT_STATUS.md).
- Do not send `vibe/` to `upstream`.
